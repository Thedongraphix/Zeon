{
  "name": "zeon-protocol",
  "version": "1.0.0",
  "description": "Zeon Protocol - AI Agent with XMTP Integration",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "yarn install && yarn workspace zeon-frontend install && yarn workspace zeon-hybrid-api install",
    "dev:frontend": "yarn workspace zeon-frontend start",
    "dev:backend": "yarn workspace zeon-hybrid-api dev",
    "build:frontend": "yarn workspace zeon-frontend build",
    "build:backend": "yarn workspace zeon-hybrid-api build",
    "start:backend": "yarn workspace zeon-hybrid-api start",
    "clean": "rm -rf node_modules && rm -rf frontend/node_modules && rm -rf backend/node_modules"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "yarn@4.6.0",
  "license": "MIT"
}
